<h1>Games</h1>

<ul>
  <% @games.each do |game| %>
    <li>
      <%= link_to game.name, game %> - <%= link_to "Destroy", game_path(game), method: :delete, data: { confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>

<%= link_to "New Game", new_game_path %>