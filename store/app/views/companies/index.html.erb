<h1>Companies</h1>

<ul>
  <% @companies.each do |company| %>
    <li>
      <%= link_to company.name, company %> - <%= link_to "Destroy", company_path(company), method: :delete, data: { confirm: "Are you sure?" } %>
    </li>
  <% end %>
</ul>

<%= link_to "New Company", new_company_path %>